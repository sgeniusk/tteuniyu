/**
 * eval-summary.ts
 *
 * Runs the summary prompt against golden cases (prompts/evals/summary.golden.jsonl)
 * and asserts copy ratio ≤ 15% per PRD v1.6 constraint.
 *
 * Status: PLACEHOLDER (Sprint 0 T-005 will implement).
 */

console.log('[eval:summary] placeholder — will run golden cases in T-005.')
process.exit(0)
