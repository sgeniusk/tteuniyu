/**
 * regression-suite.ts
 *
 * Injects intentional PRD violations, runs the relevant harness, expects
 * the harness to FAIL. Verifies harness catches known regressions.
 *
 * Status: PLACEHOLDER (Sprint 0 T-001 will populate scenarios).
 */

console.log('[harness:regression] Regression suite — placeholder (no scenarios yet).')
console.log('See harness/checks/README.md for scenarios to add in Sprint 0.')
process.exit(0)
